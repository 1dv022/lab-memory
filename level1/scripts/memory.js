window.memory = function(rows, cols, container){
	'use strict';

	var app = {

		init: function () {
			console.log("Started " + rows + "x" + cols + " memory in " + container.id);
		}

	};

	app.init();
};

